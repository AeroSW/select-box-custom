import { ItemVisibilityDirective } from './item-visibility.directive';

describe('ItemVisibilityDirective', () => {
  it('should create an instance', () => {
    const directive = new ItemVisibilityDirective();
    expect(directive).toBeTruthy();
  });
});
