import { ListVisibilityDirective } from './list-visibility.directive';

describe('ListVisibilityDirective', () => {
  it('should create an instance', () => {
    const directive = new ListVisibilityDirective();
    expect(directive).toBeTruthy();
  });
});
